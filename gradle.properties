# Configurações Gradle compartilhadas (commitadas no repo).
# Para org.gradle.java.home (específico por máquina), use ~/.gradle/gradle.properties

# Daemon reutilizável: builds subsequentes ficam ~3x mais rápidos
org.gradle.daemon=true
